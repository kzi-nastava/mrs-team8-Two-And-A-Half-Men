import { TabIconDirective } from './tab-icon.directive';

describe('TabIconDirective', () => {
  it('should create an instance', () => {
    const directive = new TabIconDirective();
    expect(directive).toBeTruthy();
  });
});
